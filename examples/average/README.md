# Average example

Tiny sample project used by Assentor Phase 10 end-to-end tests.

Starter state: `average()` throws `not implemented`.

Acceptance criteria:

1. `average(numbers)` returns the arithmetic mean
2. Empty array returns `0`
3. `node --test` passes
